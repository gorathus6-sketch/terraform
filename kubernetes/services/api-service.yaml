apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: bank-app
  labels:
    app: api
  spec:
    type: ClusterIP
    selector:
      app: api
    ports:
    - port: 80
      targetPort: 8080
