apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: bank-app
  labels:
    app: worker
  spec:
    type: ClusterIP
    selector:
      app: worker
    ports:
    - port: 8081
      targetPort: 8081