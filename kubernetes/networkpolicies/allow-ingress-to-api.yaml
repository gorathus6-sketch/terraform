apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-ingress-to-api
  namespace: bank-app
spec:
  podSelector:
    matchLabels:
      app: api
  ingress:
  - from:
    namespaceSelector:
      matchLabels:
        kubernetes.io/metadata.name: ingress-nginx