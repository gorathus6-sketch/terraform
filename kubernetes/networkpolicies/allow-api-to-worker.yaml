apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-api-to-worker
  namespace: bank-app
spec:
  podSelector:
    matchLabels:
      app: worker
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: api